{
  "name": "creative-calendar-miniapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:wx": "echo 'use HBuilderX to run mp-weixin' && exit 0",
    "build:wx": "echo 'use HBuilderX to build mp-weixin' && exit 0",
    "lint": "eslint . --ext .ts,.vue",
    "typecheck": "vue-tsc --noEmit",
    "audit:routes": "node tools/route-audit.mjs",
    "fix:routes": "node tools/route-audit.mjs --fix"
  },
  "lint-staged": {
    "*.{ts,js,vue}": "eslint --fix",
    "*.{json,md,yml,yaml}": "prettier --write"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.4.2",
    "eslint": "^8.57.0",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vue-tsc": "^2.0.12"
  }
}
